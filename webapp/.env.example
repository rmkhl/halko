# Development mode - direct localhost access
# Services must be running locally on their default ports
# API_PREFIX=

# Production mode - use nginx proxy routing
# Uncomment to use relative paths that nginx will proxy to backend services
# API_PREFIX=/api/v1
